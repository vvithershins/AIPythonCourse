```python
#Create an empty dictionary
ShoppingList = {}


addToList = input("Add what?")

howMany = int(input("How many?"))

#add the name of the item and the quantity of that item to the list as a key value pair
ShoppingList[addToList] = howMany

print(ShoppingList)
```
